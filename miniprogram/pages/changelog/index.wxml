<view class="page">
  <view class="changelog" wx:for="{{changelog}}" wx:key="version">
    <view>
      <text>{{item.version}}</text>
    </view>
    <view>
      <text>{{item.date}}</text>
    </view>
    <view wx:for="{{item.info}}">
      <text>{{item}}</text>
    </view>
  </view>
</view>